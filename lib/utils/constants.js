/**
 *
 * Reldens - Constants
 *
 */

const ROOT_DIR = 'generated-tile-map-elements';

const PROVIDER_ORDER = ['ollama', 'claude', 'gemini'];

const ELEMENT_TYPE = 'element';
const CLUSTER_TYPE = 'cluster';

module.exports = { ROOT_DIR, PROVIDER_ORDER, ELEMENT_TYPE, CLUSTER_TYPE };
